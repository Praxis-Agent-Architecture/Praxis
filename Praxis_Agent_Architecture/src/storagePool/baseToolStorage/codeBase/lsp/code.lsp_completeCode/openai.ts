export const openaiLspCompleteCodePractice = {
  providerName: "openai",
  source: "Codex CLI tool runtime boundary practice informs code.lsp_completeCode; Praxis executes through the shared LSP runtime.",
  directCliSupport: true,
  sideEffectPolicy: "read-only",
} as const;
