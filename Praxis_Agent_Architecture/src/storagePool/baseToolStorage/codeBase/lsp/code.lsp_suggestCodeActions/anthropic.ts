export const anthropicLspSuggestCodeActionsPractice = {
  providerName: "anthropic",
  source: "Claude Code LSPTool can request code actions; Praxis keeps this as suggestion-only.",
  directCliSupport: true,
} as const;
