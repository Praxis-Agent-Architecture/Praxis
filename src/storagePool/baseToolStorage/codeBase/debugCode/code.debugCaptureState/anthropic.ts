import { createHostExecutorCodeDebugCaptureStateProvider, type CodeDebugCaptureStateProviderPractice } from "./dependencies.js";
export const anthropicCodeDebugCaptureStatePractice: CodeDebugCaptureStateProviderPractice = { providerName: "anthropic", source: { kind: "cli", label: "Claude Code debug state observation practice" }, directCliSupport: true, sideEffectPolicy: "runtime-governed", notes: ["Capture debug state through a fixed tool; runtime owns the live debug session."], createProvider: ({ provider, executor }) => provider ?? createHostExecutorCodeDebugCaptureStateProvider(executor) };
