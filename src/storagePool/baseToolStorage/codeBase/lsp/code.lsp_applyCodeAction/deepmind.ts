export const deepmindLspApplyCodeActionPractice = {
  providerName: "deepmind",
  source: "Gemini CLI tool registry practice informs code.lsp_applyCodeAction; Praxis executes through the shared LSP runtime.",
  directCliSupport: false,
  sideEffectPolicy: "preview-only",
} as const;
