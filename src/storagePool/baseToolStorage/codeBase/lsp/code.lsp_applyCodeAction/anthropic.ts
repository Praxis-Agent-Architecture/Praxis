export const anthropicLspApplyCodeActionPractice = {
  providerName: "anthropic",
  source: "Claude Code LSPTool practice informs code.lsp_applyCodeAction; Praxis executes through the shared LSP runtime.",
  directCliSupport: false,
  sideEffectPolicy: "preview-only",
} as const;
