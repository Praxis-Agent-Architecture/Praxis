export const deepmindLspCompleteCodePractice = {
  providerName: "deepmind",
  source: "Gemini CLI tool registry practice informs code.lsp_completeCode; Praxis executes through the shared LSP runtime.",
  directCliSupport: true,
  sideEffectPolicy: "read-only",
} as const;
