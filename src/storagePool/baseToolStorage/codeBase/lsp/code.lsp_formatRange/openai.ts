export const openaiLspFormatRangePractice = {
  providerName: "openai",
  source: "Codex CLI tool runtime boundary practice informs code.lsp_formatRange; Praxis executes through the shared LSP runtime.",
  directCliSupport: true,
  sideEffectPolicy: "preview-only",
} as const;
