export const anthropicLspFormatRangePractice = {
  providerName: "anthropic",
  source: "Claude Code LSPTool practice informs code.lsp_formatRange; Praxis executes through the shared LSP runtime.",
  directCliSupport: true,
  sideEffectPolicy: "preview-only",
} as const;
