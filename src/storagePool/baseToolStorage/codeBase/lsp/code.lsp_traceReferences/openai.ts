export const openaiLspTraceReferencesPractice = {
  providerName: "openai",
  source: "Codex contributes runtime boundary practice; Praxis executes references through shared LSP runtime.",
  directCliSupport: false,
} as const;
