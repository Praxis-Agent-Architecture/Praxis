export const anthropicLspLocateTypeDefinitionPractice = {
  providerName: "anthropic",
  source: "Claude Code LSPTool maps type definition requests through the shared LSP runtime pattern.",
  directCliSupport: true,
} as const;
