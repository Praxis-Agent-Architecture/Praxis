export const openaiLspLocateTypeDefinitionPractice = {
  providerName: "openai",
  source: "Codex contributes the registry/runtime boundary; Praxis executes via the shared LSP runtime.",
  directCliSupport: false,
} as const;
