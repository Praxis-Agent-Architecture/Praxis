export const anthropicLspInspectDiagnosticsPractice = {
  providerName: "anthropic",
  source: "Claude Code LSPTool practice informs code.lsp_inspectDiagnostics; Praxis executes through the shared LSP runtime.",
  directCliSupport: true,
  sideEffectPolicy: "read-only",
} as const;
