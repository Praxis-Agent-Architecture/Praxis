export const anthropicLspAssistSignaturePractice = {
  providerName: "anthropic",
  source: "Claude Code LSPTool practice informs code.lsp_assistSignature; Praxis executes through the shared LSP runtime.",
  directCliSupport: true,
  sideEffectPolicy: "read-only",
} as const;
