export const anthropicLspRenameSymbolPractice = {
  providerName: "anthropic",
  source: "Claude Code LSPTool can request rename edits; Praxis keeps this as preview-only.",
  directCliSupport: true,
} as const;
