export const openaiLspApplyCodeActionPractice = {
  providerName: "openai",
  source: "Codex CLI tool runtime boundary practice informs code.lsp_applyCodeAction; Praxis executes through the shared LSP runtime.",
  directCliSupport: false,
  sideEffectPolicy: "preview-only",
} as const;
