export const anthropicLspSearchWorkspaceSymbolsPractice = {
  providerName: "anthropic",
  source: "Claude Code LSPTool maps workspace symbol lookup to workspace/symbol.",
  directCliSupport: true,
} as const;
