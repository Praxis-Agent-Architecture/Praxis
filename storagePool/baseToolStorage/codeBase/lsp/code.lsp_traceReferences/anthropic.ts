export const anthropicLspTraceReferencesPractice = {
  providerName: "anthropic",
  source: "Claude Code LSPTool maps reference lookup to textDocument/references.",
  directCliSupport: true,
} as const;
