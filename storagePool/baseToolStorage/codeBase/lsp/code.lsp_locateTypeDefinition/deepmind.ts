export const deepmindLspLocateTypeDefinitionPractice = {
  providerName: "deepmind",
  source: "Gemini CLI contributes declaration/execution separation; Praxis executes via the shared LSP runtime.",
  directCliSupport: false,
} as const;
