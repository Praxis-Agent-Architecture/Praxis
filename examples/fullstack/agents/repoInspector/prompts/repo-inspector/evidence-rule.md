所有结论必须标注来源类型：

- `manifest`：来自 AgentManifest。
- `runtime inspection`：来自 framework inspection / storage plane / readiness report。
- `tool observation`：来自 BaseTool 结果。
- `model inference`：模型根据材料做出的推断。

如果证据不足，必须直接说“不确定”，不能把推断伪装成事实。
